# 🔐 Brandscaling Authentication Flow Diagram

## 📊 Complete User Journey

```
┌─────────────────────────────────────────────────────────────────────┐
│                                                                     │
│                        USER VISITS APP                              │
│                      (First time or return)                         │
│                                                                     │
└──────────────────────────┬──────────────────────────────────────────┘
                           │
                           ▼
                    ┌──────────────┐
                    │ App.tsx      │
                    │ useEffect()  │
                    └──────┬───────┘
                           │
                           ▼
              ┌────────────────────────┐
              │  checkSession()        │
              │  authHelpers.          │
              │    getSession()        │
              └────────┬───────────────┘
                       │
       ┌───────────────┴────────────────┐
       │                                │
       ▼                                ▼
┌─────────────┐                  ┌──────────────┐
│ HAS SESSION │                  │  NO SESSION  │
│  (Logged In)│                  │ (New/Logout) │
└──────┬──────┘                  └──────┬───────┘
       │                                │
       │                                │
       ▼                                ▼
┌─────────────────┐            ┌─────────────────┐
│ setUser(...)    │            │ Show Home Page  │
│ setIsAuth(true) │            │ with "Sign In"  │
│                 │            │ button          │
│ Load Quiz       │            └────────┬────────┘
│ Results from    │                     │
│ Backend         │                     │
└────────┬────────┘                     │
         │                              │
         │                              ▼
         │                    ┌──────────────────┐
         │                    │ User clicks      │
         │                    │ "Take Quiz" or   │
         │                    │ "Sign In"        │
         │                    └─────────┬────────┘
         │                              │
         │                              ▼
         │                    ┌──────────────────┐
         │                    │ Show AuthScreens │
         │                    │ Component        │
         │                    └─────────┬────────┘
         │                              │
         │              ┌───────────────┴────────────────┐
         │              │                                │
         │              ▼                                ▼
         │      ┌──────────────┐                ┌──────────────┐
         │      │ LOGIN MODE   │                │ SIGNUP MODE  │
         │      └──────┬───────┘                └──────┬───────┘
         │             │                               │
         │             ▼                               ▼
         │   ┌──────────────────┐          ┌────────────────────┐
         │   │ Enter email &    │          │ Enter name, email, │
         │   │ password         │          │ password, confirm  │
         │   └────────┬─────────┘          └─────────┬──────────┘
         │            │                              │
         │            ▼                              ▼
         │   ┌──────────────────┐          ┌────────────────────┐
         │   │ authHelpers.     │          │ Validate inputs    │
         │   │   signIn()       │          │ (client-side)      │
         │   │                  │          └─────────┬──────────┘
         │   └────────┬─────────┘                    │
         │            │                              ▼
         │            │                  ┌────────────────────────┐
         │            │                  │ POST /signup to server │
         │            │                  │ (server-side creation) │
         │            │                  └─────────┬──────────────┘
         │            │                            │
         │            │                            ▼
         │            │              ┌──────────────────────────┐
         │            │              │ Server validates inputs  │
         │            │              │ Creates user with        │
         │            │              │ SERVICE_ROLE_KEY         │
         │            │              │ email_confirm: true      │
         │            │              └─────────┬────────────────┘
         │            │                        │
         │            │                        ▼
         │            │              ┌──────────────────────────┐
         │            │              │ User created in Supabase │
         │            │              │ Auto-confirmed           │
         │            │              └─────────┬────────────────┘
         │            │                        │
         │            │                        ▼
         │            │              ┌──────────────────────────┐
         │            │              │ Client calls signIn()    │
         │            │              │ with same credentials    │
         │            │              └─────────┬────────────────┘
         │            │                        │
         │            └────────────────────────┘
         │                         │
         │                         ▼
         │              ┌────────────────────────┐
         │              │ Supabase Auth verifies │
         │              │ Returns session token  │
         │              └───────────┬────────────┘
         │                          │
         │                          ▼
         │              ┌────────────────────────┐
         │              │ onAuthStateChange()    │
         │              │ listener fires         │
         │              └───────────┬────────────┘
         │                          │
         │                          ▼
         │              ┌────────────────────────┐
         │              │ setUser(session.user)  │
         │              │ setIsAuth(true)        │
         │              └───────────┬────────────┘
         │                          │
         └──────────────────────────┘
                    │
                    ▼
         ┌────────────────────────┐
         │ Load Quiz Results      │
         │ GET /quiz-results/:id  │
         └───────────┬────────────┘
                     │
                     ▼
         ┌────────────────────────┐
         │ If results exist:      │
         │   setQuizResults(...)  │
         │ Else:                  │
         │   Show onboarding      │
         └───────────┬────────────┘
                     │
                     ▼
         ┌────────────────────────┐
         │ User sees Dashboard    │
         │ or Quiz based on state │
         └────────────────────────┘
```

---

## 🔄 Session Lifecycle

```
┌─────────────────────────────────────────────────────────────────┐
│                     SESSION PERSISTENCE                         │
└─────────────────────────────────────────────────────────────────┘

LOGIN
  ↓
┌───────────────────────────────────┐
│ Session Token Created             │
│ • Stored in localStorage          │
│ • Contains user ID, email, exp    │
│ • Auto-refresh enabled            │
└──────────────┬────────────────────┘
               │
               ▼
     ┌─────────────────┐
     │ USER ACTIVE     │
     │ (browsing app)  │
     └────────┬────────┘
              │
              │ Page Refresh?
              │
              ▼
     ┌──────────────────┐
     │ getSession()     │
     │ Check localStorage│
     └────────┬─────────┘
              │
      ┌───────┴────────┐
      │                │
      ▼                ▼
┌──────────┐    ┌──────────┐
│ VALID    │    │ EXPIRED  │
│ SESSION  │    │ or NONE  │
└────┬─────┘    └────┬─────┘
     │               │
     │               ▼
     │         ┌───────────┐
     │         │ LOGGED    │
     │         │ OUT       │
     │         └───────────┘
     │
     ▼
┌──────────────┐
│ Auto-refresh │ <─── Before expiry
│ token if     │
│ needed       │
└──────┬───────┘
       │
       ▼
┌──────────────┐
│ STAY LOGGED  │
│ IN           │
└──────────────┘
```

---

## 📝 Quiz Results Sync Flow

```
┌─────────────────────────────────────────────────────────────┐
│                    QUIZ COMPLETION                          │
└─────────────────────────────────────────────────────────────┘

User completes quiz
     │
     ▼
┌──────────────────────┐
│ Calculate results    │
│ (Layers 1-7)         │
│ Architect/Alchemist  │
│ Subtype, scores      │
└──────────┬───────────┘
           │
           ▼
┌──────────────────────┐
│ setQuizResults()     │
│ (local React state)  │
└──────────┬───────────┘
           │
           ▼
    ┌──────────────┐
    │ User logged  │
    │ in?          │
    └──────┬───────┘
           │
    ┌──────┴──────┐
    │             │
    ▼             ▼
┌────────┐   ┌─────────┐
│  YES   │   │   NO    │
└───┬────┘   └────┬────┘
    │             │
    │             ▼
    │        ┌─────────────┐
    │        │ Results in  │
    │        │ memory only │
    │        │ (lost on    │
    │        │  reload)    │
    │        └─────────────┘
    │
    ▼
┌──────────────────────────┐
│ POST /quiz-results       │
│ {                        │
│   user_id: "...",        │
│   results: {...}         │
│ }                        │
└──────────┬───────────────┘
           │
           ▼
┌──────────────────────────┐
│ Server saves to KV store │
│ Key: quiz_results:{id}   │
│ Value: {results, date}   │
└──────────┬───────────────┘
           │
           ▼
┌──────────────────────────┐
│ ✅ Results persisted     │
│ Available on:            │
│ • Page reload            │
│ • Different devices      │
│ • Future sessions        │
└──────────────────────────┘
```

---

## 🔐 Security Layers

```
┌─────────────────────────────────────────────────────────────┐
│                      SECURITY MODEL                         │
└─────────────────────────────────────────────────────────────┘

CLIENT (Browser)
├── Public Anon Key (✅ Safe to expose)
│   └── Can: Read public data, sign in, sign up via server
│   └── Cannot: Create users directly, delete data
│
SERVER (Edge Function)
├── Service Role Key (🔒 SECRET)
│   └── Can: Create users, admin operations
│   └── Used for: Signup endpoint only
│
SUPABASE (Database)
└── Row Level Security (RLS)
    └── Policy: Users can only read/write their own data
    └── Enforced at database level

┌─────────────────────────────────────────────────────────────┐
│                   REQUEST FLOW                              │
└─────────────────────────────────────────────────────────────┘

1. SIGNUP
   Client → Server (with Anon Key)
   Server → Supabase (with Service Role Key)
   
2. LOGIN
   Client → Supabase (with Anon Key)
   
3. QUIZ SAVE
   Client → Server (with Session Token)
   Server validates token → Saves data
   
4. QUIZ LOAD
   Client → Server (with Session Token)
   Server validates token → Returns user's data only
```

---

## 🎯 State Management

```
┌─────────────────────────────────────────────────────────────┐
│                APP STATE (App.tsx)                          │
└─────────────────────────────────────────────────────────────┘

const [isAuthenticated, setIsAuthenticated] = useState(false)
     │
     ├─→ false: Show login prompt on protected routes
     └─→ true: Allow access to quiz, dashboard, chat

const [user, setUser] = useState<User | null>(null)
     │
     ├─→ null: Not logged in
     └─→ { email, name, id }: Logged in
         │
         └─→ id used for quiz results key

const [quizResults, setQuizResults] = useState<EDNAResults | null>(null)
     │
     ├─→ null: Haven't taken quiz, show onboarding
     └─→ {...}: Quiz completed, show personalized content

const [currentView, setCurrentView] = useState('home')
     │
     ├─→ 'home': Landing page
     ├─→ 'quiz': E-DNA assessment (requires auth)
     ├─→ 'results': Quiz results page
     ├─→ 'dashboard': Personalized LMS (requires auth + quiz)
     └─→ 'chat': AI mentor (requires auth + quiz)

┌─────────────────────────────────────────────────────────────┐
│                   VIEW GUARDS                               │
└─────────────────────────────────────────────────────────────┘

handleViewChange(view) {
  if (!isAuthenticated && view in ['quiz', 'dashboard', 'chat']) {
    → Show AuthScreens
  }
  
  if (view === 'quiz' && !quizResults) {
    → Show OnboardingFlow first
  }
  
  Otherwise: setCurrentView(view)
}
```

---

## 🎨 Component Hierarchy

```
App.tsx
├── Navigation
│   ├── Brand logo
│   ├── Nav links (Home, Quiz, Dashboard, Chat)
│   └── Auth button (Login/Logout)
│
├── [Conditional Rendering based on state]
│
├── AuthScreens (if showAuth === true)
│   ├── LoginForm
│   │   ├── Email input
│   │   ├── Password input
│   │   └── Submit → authHelpers.signIn()
│   │
│   └── RegisterForm
│       ├── Name inputs
│       ├── Email input
│       ├── Password inputs
│       └── Submit → POST /signup → authHelpers.signIn()
│
├── OnboardingFlow (if showOnboarding === true)
│   └── Welcome screens → Start Quiz
│
├── Home (if currentView === 'home')
│   └── Hero, features, CTA
│
├── EDNAQuiz (if currentView === 'quiz' && isAuthenticated)
│   └── 56 questions across 7 layers
│   └── onComplete → saveQuizResults()
│
├── EDNAResultsPage (if currentView === 'results' && quizResults)
│   ├── Persona display
│   ├── Scores breakdown
│   ├── PDF export
│   └── Share buttons
│
├── PersonalizedLMS (if currentView === 'dashboard' && quizResults)
│   └── Courses tailored to persona
│
└── PersonalizedAIChat (if currentView === 'chat' && quizResults)
    └── AI mentor matching persona
```

---

## ⚡ Performance Optimizations

```
┌─────────────────────────────────────────────────────────────┐
│              AUTHENTICATION OPTIMIZATIONS                   │
└─────────────────────────────────────────────────────────────┘

1. Session Check
   ✅ Only runs once on app mount
   ✅ Cached result prevents repeated checks
   ✅ Loading state shows spinner during check

2. Quiz Results Loading
   ✅ Non-blocking (app still usable during load)
   ✅ Silent failure (user can retake quiz if load fails)
   ✅ Cached in React state after first load

3. Auth State Listener
   ✅ Subscription cleaned up on unmount
   ✅ Prevents memory leaks
   ✅ Only fires on actual auth changes

4. Backend Health Check
   ✅ Runs once per session
   ✅ Doesn't block UI rendering
   ✅ Clear console messaging

5. Graceful Degradation
   ✅ App works without backend
   ✅ Results stored in local state
   ✅ User informed of limitations
   ✅ No scary error messages
```

---

## 🚨 Error Scenarios Handled

```
┌─────────────────────────────────────────────────────────────┐
│                   ERROR HANDLING MATRIX                     │
└─────────────────────────────────────────────────────────────┘

SCENARIO                    | HANDLING
─────────────────────────────────────────────────────────────
Backend not deployed        | ✅ Graceful message, app works
Invalid credentials         | ✅ User-friendly error display
Email already exists        | ✅ Clear message with action
Network timeout             | ✅ Retry prompt
Session expired             | ✅ Auto-logout, redirect to login
Quiz save fails             | ✅ Silent, results in local state
Quiz load fails             | ✅ Silent, user can retake
Invalid email format        | ✅ Client-side validation
Weak password               | ✅ Client-side validation
Password mismatch           | ✅ Client-side validation
Server error 500            | ✅ Generic error with retry option
CORS error                  | ✅ Server properly configured
Token expired               | ✅ Auto-refresh before expiry
```

---

## 📱 Responsive Behavior

```
DESKTOP (1024px+)
├── Full navigation bar
├── Auth modal centered
├── Wide quiz layout
└── Dashboard 3-column grid

TABLET (768px - 1023px)
├── Condensed navigation
├── Auth modal adjusted
├── Quiz 2-column layout
└── Dashboard 2-column grid

MOBILE (< 768px)
├── Hamburger menu
├── Full-screen auth
├── Quiz single column
└── Dashboard stacked cards
```

---

This completes the comprehensive authentication flow documentation! 🎉
